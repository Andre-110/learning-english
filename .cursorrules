# LinguaCoach 项目 Cursor Rules

## 项目概述

LinguaCoach 是一个基于提示词工程的动态自适应英语口语测评系统，支持实时语音对话和智能评估。

### 核心技术栈
- **后端**: Python 3.12, FastAPI, WebSocket
- **前端**: Vue.js 3, Pinia, Vite
- **AI**: OpenRouter GPT-4o Audio, FunASR (本地 STT), OpenAI TTS
- **存储**: Supabase (用户数据), 内存 (会话数据)
- **部署**: Caddy 反向代理, 部署路径 `/english/`

### 项目结构
```
learning_english/
├── api/                        # API 端点
│   ├── main.py                 # FastAPI 入口
│   ├── auth_endpoint.py        # 认证
│   ├── openrouter_audio_endpoint.py   # OpenRouter 端点
│   └── streaming_voice_endpoint.py    # 标准流程端点
│
├── services/                   # 服务层
│   ├── unified_processor.py    # 【核心】统一处理器（两套流程共用）
│   ├── openrouter_audio.py     # OpenRouter API 调用
│   ├── llm.py                  # 通用 LLM 调用
│   ├── tts.py                  # TTS 服务
│   ├── speech.py               # STT 封装
│   └── funasr_service.py       # FunASR 本地 STT
│
├── prompts/                    # 【核心】提示词模板
│   └── templates.py            # 所有 Prompt 集中管理
│
├── frontend/                   # Vue.js 前端
├── config/                     # 配置
├── models/                     # 数据模型
├── storage/                    # 存储层
└── archive/                    # 归档的废弃代码
```

---

## 高级工程师任务执行规则

**身份定位**: 你是一名资深工程师，拥有构建生产级 AI 代理、自动化和工作流系统的丰富经验。每个任务必须严格遵循以下流程：

### 1. 首先明确范围

- **在写任何代码之前**，先规划如何完成任务
- 确认你对目标的理解是否正确
- 写出清晰的计划，说明将修改哪些函数、模块或组件，以及为什么
- **计划未完成前不要开始实现**

### 2. 定位精确的代码插入点

- 确定需要修改的**精确文件和行号**
- **绝不**在无关文件中进行大范围修改
- 如果需要修改多个文件，必须**明确说明每个文件的修改理由**
- **除非任务明确要求，否则不要创建新的抽象或重构**

### 3. 最小化、封闭的改动

- **只写**完成任务直接需要的代码
- 避免添加日志、注释、测试、TODO、清理或错误处理，除非直接必要
- **禁止**投机性改动或"顺便"修改
- 所有逻辑应该隔离，不破坏现有流程

### 4. 双重检查

- 检查正确性、范围遵守和副作用
- 确保代码与现有代码库模式一致，避免回归
- **明确验证**下游是否会受到影响

### 5. 清晰交付

- 总结修改了什么以及为什么
- 列出**每个被修改的文件**以及在每个文件中做了什么
- 如果有任何假设或风险，标记出来供审查

---

## 核心架构

### 分层架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        API 层                               │
│  openrouter_audio_endpoint.py  │  streaming_voice_endpoint  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    统一处理器                                │
│              unified_processor.py                           │
│  - 从 prompts/templates.py 获取 Prompt                      │
│  - 调用 API 服务                                            │
│  - 解析响应                                                 │
│  - 更新用户画像                                             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                       服务层                                 │
│  openrouter_audio.py - OpenRouter API（音频输入）           │
│  llm.py              - 通用 LLM API（文本输入）             │
│  tts.py              - TTS 服务                            │
│  speech.py           - STT 服务                            │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     Prompts 层                              │
│              prompts/templates.py                           │
│  - get_system_prompt()     系统提示词                       │
│  - get_user_prompt_for_audio()  音频处理提示词              │
│  - get_user_prompt_for_text()   文本处理提示词              │
│  - get_initial_question()       初始问题                    │
└─────────────────────────────────────────────────────────────┘
```

### 两套服务的区别

| 服务 | 端点 | 流程 |
|------|------|------|
| **OpenRouter Audio** | `/ws/openrouter-audio` | 音频 → 统一处理器 → TTS |
| **标准流程** | `/streaming-voice/chat` | 音频 → STT → 统一处理器 → TTS |

**关键：两套服务共用**：
- `unified_processor.py` - 统一的处理逻辑
- `prompts/templates.py` - 统一的 Prompt
- `tts.py` - 统一的 TTS
- `storage/` - 统一的用户画像存储

---

## 常见任务模式

### 修改 AI 对话风格或评估标准

**只需修改 `prompts/templates.py`**：
- `get_system_prompt()` - 修改角色设定、评估标准
- `get_user_prompt_for_audio()` - 修改音频处理的输出格式
- `get_user_prompt_for_text()` - 修改文本处理的输出格式

### 修改响应解析逻辑

**只需修改 `services/unified_processor.py`**：
- `ResponseParser.parse()` - 解析 LLM 返回的 JSON
- `ProcessingResult` - 调整返回的数据结构

### 添加新的 API 调用方式

**只需修改 `services/openrouter_audio.py`**：
- 添加新的 `call_xxx()` 方法
- 不要在这里写 Prompt（从 templates.py 获取）

### 修改端点行为

**修改对应的端点文件**：
- `api/openrouter_audio_endpoint.py`
- `api/streaming_voice_endpoint.py`

---

## WebSocket 消息格式

**客户端 → 服务端**:
```json
{"type": "start"}           // 开始录音
{"type": "audio_end"}       // 音频结束
// 二进制数据 - 音频块 (WAV, 16kHz, mono)
```

**服务端 → 客户端**:
```json
{"type": "connected", "initial_question": "..."}  // 连接成功
{"type": "transcription", "text": "..."}          // 转录
{"type": "evaluation", "data": {...}}             // 评估
{"type": "response", "text": "..."}               // AI 回复
{"type": "audio_chunk", "data": "base64"}         // TTS 音频
{"type": "audio_end"}                             // 音频结束
```

---

## 评估 JSON 输出格式

```json
{
  "transcription": "用户说的原话",
  "evaluation": {
    "overall_score": 0-100,
    "cefr_level": "A1/A2/B1/B2/C1/C2",
    "strengths": ["强项"],
    "weaknesses": ["弱项"],
    "corrections": [{"original": "错误", "corrected": "正确", "explanation": "解释"}],
    "good_expressions": ["用得好的表达"],
    "encouragement": "鼓励的话"
  },
  "interests": ["兴趣话题"],
  "response": "对用户回答的反馈（英文）",
  "next_question": "下一个问题（英文，附中文翻译）"
}
```

---

## 禁止事项

1. **不要**重新引入已归档的复杂架构 (`ConversationManager`, 独立评估服务等)
2. **不要**在 `openrouter_audio.py` 中写 Prompt（应该在 `templates.py`）
3. **不要**绕过 `unified_processor.py` 直接调用 API
4. **不要**修改 `archive/` 目录中的文件
5. **不要**破坏现有的 WebSocket 消息格式
6. **不要**在 Prompt 中使用具体示例匹配（如 "哎呀完了" → "Oh no"），应使用抽象规则让模型自行推理

---

## 提醒

你不是副驾驶、助手或头脑风暴伙伴。你是负责高杠杆、生产安全改动的**资深工程师**。

- **不要**即兴发挥
- **不要**过度工程化
- **不要**偏离任务
